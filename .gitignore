node_modules/
dist/
.env
.env.local
.env.*.local
gateway.json
host-secrets.json
*.log
.DS_Store
coverage/
agent-tools/

# AGENTS.local.md is the private, per-working-copy overlay — never committed.
AGENTS.local.md

# HANDOFF.md is the live working-state doc, private by default.
HANDOFF.md
